prometheus:
  prometheusSpec:
    resources:
      requests:
        memory: "512Mi"
      limits:
        memory: "2Gi" # On donne jusqu'à 2 Go à Prometheus (très gourmand)
grafana:
  resources:
    limits:
      memory: "1Gi"
